<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dr Ikponmwosa Irene Practice - Medical Invoice</title>
    <style>
        @media print {
            body { margin: 0; padding: 0; background: white; }
            .no-print { display: none !important; }
            .page { box-shadow: none; margin: 0; }
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            background: linear-gradient(135deg, #0d47a1 0%, #4a148c 30%, #b71c1c 60%, #4a148c 80%, #0d47a1 100%);
            margin: 0;
            padding: 30px 20px;
            min-height: 100vh;
        }

        /* === BUTTONS === */
        .no-print {
            text-align: center;
            max-width: 210mm;
            margin: 0 auto 25px auto;
        }

        .btn {
            background: linear-gradient(135deg, #ffffff, #e8eaf6);
            color: #1a237e;
            border: 2px solid #ffffff;
            padding: 16px 40px;
            font-size: 18px;
            font-weight: bold;
            border-radius: 50px;
            cursor: pointer;
            margin: 5px 10px;
            transition: all 0.3s;
            letter-spacing: 1px;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255,255,255,0.3);
        }

        .btn-pdf {
            background: linear-gradient(135deg, #e91e63, #c62828);
            color: white;
            border-color: #e91e63;
        }

        .btn-pdf:hover {
            background: linear-gradient(135deg, #c62828, #e91e63);
            border-color: white;
        }

        /* === A4 PAGE === */
        .page {
            width: 210mm;
            min-height: 297mm;
            background: #ffffff;
            margin: 0 auto;
            padding: 0;
            box-shadow: 0 10px 40px rgba(0,0,0,0.3);
            position: relative;
            overflow: hidden;
        }

        /* === BACKGROUND === */
        .bg-pattern {
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            opacity: 0.03;
            z-index: 0;
            pointer-events: none;
            background-image:
                radial-gradient(circle at 25% 25%, #1a237e 1px, transparent 1px),
                radial-gradient(circle at 75% 75%, #4a148c 1px, transparent 1px);
            background-size: 35px 35px, 45px 45px;
        }

        /* === SIDE STRIPES === */
        .side-left {
            position: absolute;
            top: 0; left: 0;
            width: 14px; height: 100%;
            background: linear-gradient(to bottom,
                #1a237e 0%, #4a148c 25%, #e91e63 50%, #4a148c 75%, #1a237e 100%);
            z-index: 2;
        }

        .side-right {
            position: absolute;
            top: 0; right: 0;
            width: 14px; height: 100%;
            background: linear-gradient(to bottom,
                #1a237e 0%, #4a148c 25%, #e91e63 50%, #4a148c 75%, #1a237e 100%);
            z-index: 2;
        }

        /* === CORNERS === */
        .corner-tl {
            position: absolute; top: 10px; left: 20px;
            width: 50px; height: 50px;
            border-top: 3px solid rgba(26,35,126,0.1);
            border-left: 3px solid rgba(26,35,126,0.1);
            border-top-left-radius: 15px; z-index: 1;
        }
        .corner-tr {
            position: absolute; top: 10px; right: 20px;
            width: 50px; height: 50px;
            border-top: 3px solid rgba(26,35,126,0.1);
            border-right: 3px solid rgba(26,35,126,0.1);
            border-top-right-radius: 15px; z-index: 1;
        }
        .corner-bl {
            position: absolute; bottom: 55px; left: 20px;
            width: 50px; height: 50px;
            border-bottom: 3px solid rgba(26,35,126,0.1);
            border-left: 3px solid rgba(26,35,126,0.1);
            border-bottom-left-radius: 15px; z-index: 1;
        }
        .corner-br {
            position: absolute; bottom: 55px; right: 20px;
            width: 50px; height: 50px;
            border-bottom: 3px solid rgba(26,35,126,0.1);
            border-right: 3px solid rgba(26,35,126,0.1);
            border-bottom-right-radius: 15px; z-index: 1;
        }

        /* === WATERMARKS === */
        .watermark-symbol {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            font-size: 280px;
            color: rgba(26, 35, 126, 0.02);
            pointer-events: none; z-index: 0;
        }

        .watermark-text {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%) rotate(-25deg);
            font-size: 60px;
            color: rgba(74, 20, 140, 0.02);
            pointer-events: none; z-index: 0;
            letter-spacing: 15px;
            font-weight: bold;
            white-space: nowrap;
        }

        /* === INNER === */
        .inner {
            position: relative;
            z-index: 1;
            padding: 0 45px 35px 50px;
        }

        /* === HEADER === */
        .header-bar {
            background: linear-gradient(135deg, #1a237e 0%, #283593 20%, #4a148c 50%, #6a1b9a 80%, #4a148c 100%);
            margin: 0 -45px 0 -50px;
            padding: 25px 50px 22px 55px;
            text-align: center;
        }

        .header-cross {
            width: 60px; height: 60px;
            background: rgba(255,255,255,0.12);
            border: 3px solid rgba(255,255,255,0.5);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 10px auto;
        }

        .header-cross span {
            font-size: 35px;
            color: #e91e63;
            font-weight: bold;
        }

        .practice-name {
            font-size: 26px;
            font-weight: bold;
            color: #ffffff;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 3px;
        }

        .medical-title {
            font-size: 18px;
            color: #e8b4f8;
            font-style: italic;
            letter-spacing: 2px;
        }

        /* === BADGES === */
        .badges {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin: 18px 0 15px 0;
            flex-wrap: wrap;
        }

        .badge {
            background: linear-gradient(135deg, #1a237e, #283593);
            color: white;
            padding: 5px 18px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: bold;
            letter-spacing: 1px;
        }

        .badge.purple { background: linear-gradient(135deg, #4a148c, #6a1b9a); }
        .badge.red { background: linear-gradient(135deg, #b71c1c, #c62828); }
        .badge.green { background: linear-gradient(135deg, #1b5e20, #2e7d32); }

        /* === DETAILS BOX === */
        .details-box {
            background: linear-gradient(135deg, #f5f5ff, #ede7f6, #fce4ec, #ede7f6, #f5f5ff);
            border: 1px solid #c5cae9;
            border-left: 5px solid #4a148c;
            border-right: 5px solid #4a148c;
            border-radius: 10px;
            padding: 12px 20px;
            box-shadow: 0 3px 12px rgba(74, 20, 140, 0.06);
        }

        .details-row {
            font-size: 11.5px;
            color: #1a237e;
            line-height: 1.9;
            text-align: center;
        }

        .details-row .label { color: #4a148c; font-weight: bold; }
        .details-row .sep { color: #e91e63; font-weight: bold; margin: 0 5px; }

        /* === INVOICE TITLE === */
        .invoice-title-banner {
            background: linear-gradient(135deg, #e8eaf6, #ede7f6, #fce4ec, #ede7f6, #e8eaf6);
            border: 2px solid #1a237e;
            border-radius: 12px;
            padding: 12px;
            text-align: center;
            margin: 18px 0;
            position: relative;
        }

        .invoice-title-banner h2 {
            font-size: 24px;
            color: #1a237e;
            letter-spacing: 5px;
            text-transform: uppercase;
            margin: 0;
        }

        .invoice-title-banner .subtitle {
            font-size: 12px;
            color: #4a148c;
            font-style: italic;
            margin-top: 2px;
        }

        .invoice-title-banner::before {
            content: '';
            position: absolute;
            top: -2px; left: 25%;
            width: 50%; height: 4px;
            background: linear-gradient(to right, #e91e63, #4a148c, #e91e63);
            border-radius: 2px;
        }

        .invoice-title-banner::after {
            content: '';
            position: absolute;
            bottom: -2px; left: 25%;
            width: 50%; height: 4px;
            background: linear-gradient(to right, #4a148c, #e91e63, #4a148c);
            border-radius: 2px;
        }

        /* === DIVIDERS === */
        .fancy-divider {
            display: flex;
            align-items: center;
            margin: 15px 0;
        }

        .fancy-divider .line {
            flex: 1; height: 2px;
            background: linear-gradient(to right, transparent, #1a237e, #4a148c);
        }

        .fancy-divider .line.right {
            background: linear-gradient(to left, transparent, #1a237e, #4a148c);
        }

        .fancy-divider .diamond {
            width: 10px; height: 10px;
            background: #e91e63;
            transform: rotate(45deg);
            margin: 0 12px;
        }

        .fancy-divider .dot {
            width: 6px; height: 6px;
            background: #4a148c;
            border-radius: 50%;
            margin: 0 6px;
        }

        /* === INVOICE INFO GRID === */
        .invoice-info {
            display: flex;
            justify-content: space-between;
            gap: 20px;
            margin: 15px 0;
            flex-wrap: wrap;
        }

        .info-block {
            flex: 1;
            min-width: 200px;
        }

        .info-block-title {
            font-size: 12px;
            color: #4a148c;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            padding-bottom: 4px;
            border-bottom: 2px solid #e91e63;
            display: inline-block;
        }

        .info-field {
            display: flex;
            align-items: baseline;
            margin-bottom: 6px;
            font-size: 13px;
        }

        .info-label {
            font-weight: bold;
            color: #1a237e;
            min-width: 110px;
            font-size: 12px;
        }

        .info-value {
            flex: 1;
            border-bottom: 1px dotted #9fa8da;
            min-height: 18px;
            padding-left: 5px;
        }

        /* === INVOICE NUMBER BOX === */
        .invoice-number-box {
            background: linear-gradient(135deg, #1a237e, #283593);
            color: white;
            padding: 12px 20px;
            border-radius: 10px;
            text-align: center;
            min-width: 180px;
        }

        .invoice-number-box .inv-label {
            font-size: 11px;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .invoice-number-box .inv-number {
            font-size: 20px;
            font-weight: bold;
            letter-spacing: 2px;
            margin-top: 3px;
        }

        .invoice-number-box .inv-date {
            font-size: 11px;
            opacity: 0.8;
            margin-top: 5px;
        }

        /* === TABLE === */
        .invoice-table {
            width: 100%;
            border-collapse: collapse;
            margin: 15px 0;
            font-size: 13px;
        }

        .invoice-table thead {
            background: linear-gradient(135deg, #1a237e, #283593, #4a148c);
        }

        .invoice-table thead th {
            color: white;
            padding: 12px 10px;
            text-align: left;
            font-size: 12px;
            letter-spacing: 1px;
            text-transform: uppercase;
            font-weight: bold;
        }

        .invoice-table thead th:last-child,
        .invoice-table thead th:nth-child(3),
        .invoice-table thead th:nth-child(4) {
            text-align: right;
        }

        .invoice-table tbody tr {
            border-bottom: 1px solid #e8eaf6;
        }

        .invoice-table tbody tr:nth-child(even) {
            background: linear-gradient(135deg, #f5f5ff, #fafafa);
        }

        .invoice-table tbody tr:nth-child(odd) {
            background: white;
        }

        .invoice-table tbody td {
            padding: 14px 10px;
            color: #333;
            vertical-align: top;
        }

        .invoice-table tbody td:last-child,
        .invoice-table tbody td:nth-child(3),
        .invoice-table tbody td:nth-child(4) {
            text-align: right;
        }

        .invoice-table tbody tr:hover {
            background: #ede7f6;
        }

        /* Empty rows for writing */
        .invoice-table tbody tr.empty-row td {
            height: 35px;
            border-bottom: 1px dotted #c5cae9;
        }

        /* === TOTALS === */
        .totals-section {
            display: flex;
            justify-content: flex-end;
            margin-top: 5px;
        }

        .totals-box {
            width: 280px;
            border: 2px solid #1a237e;
            border-radius: 10px;
            overflow: hidden;
        }

        .totals-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 18px;
            font-size: 13px;
            border-bottom: 1px solid #e8eaf6;
        }

        .totals-row .t-label {
            color: #1a237e;
            font-weight: bold;
        }

        .totals-row .t-value {
            color: #333;
            min-width: 100px;
            text-align: right;
            border-bottom: 1px dotted #9fa8da;
        }

        .totals-row.total {
            background: linear-gradient(135deg, #1a237e, #4a148c);
            border-bottom: none;
        }

        .totals-row.total .t-label {
            color: white;
            font-size: 15px;
            letter-spacing: 1px;
        }

        .totals-row.total .t-value {
            color: #e8b4f8;
            font-size: 16px;
            font-weight: bold;
            border-bottom: none;
        }

        /* === PAYMENT DETAILS === */
        .payment-section {
            display: flex;
            gap: 20px;
            margin-top: 18px;
            flex-wrap: wrap;
        }

        .payment-box {
            flex: 1;
            min-width: 200px;
            background: linear-gradient(135deg, #f5f5ff, #ede7f6);
            border: 1px solid #c5cae9;
            border-left: 5px solid #1a237e;
            border-radius: 8px;
            padding: 14px 18px;
        }

        .payment-box-title {
            font-size: 13px;
            font-weight: bold;
            color: #1a237e;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
            padding-bottom: 5px;
            border-bottom: 2px solid #e91e63;
            display: inline-block;
        }

        .payment-field {
            display: flex;
            align-items: baseline;
            margin-bottom: 5px;
            font-size: 12px;
        }

        .payment-label {
            font-weight: bold;
            color: #4a148c;
            min-width: 120px;
        }

        .payment-value {
            flex: 1;
            border-bottom: 1px dotted #9fa8da;
            min-height: 16px;
            padding-left: 5px;
            color: #333;
        }

        /* === TERMS BOX === */
        .terms-box {
            flex: 1;
            min-width: 200px;
            background: linear-gradient(135deg, #fce4ec, #f3e5f5);
            border: 1px solid #e1bee7;
            border-left: 5px solid #e91e63;
            border-radius: 8px;
            padding: 14px 18px;
        }

        .terms-title {
            font-size: 13px;
            font-weight: bold;
            color: #c62828;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            padding-bottom: 5px;
            border-bottom: 2px solid #4a148c;
            display: inline-block;
        }

        .terms-text {
            font-size: 11px;
            color: #555;
            line-height: 1.7;
        }

        .terms-text li {
            margin-bottom: 3px;
        }

        /* === SIGNATURE === */
        .sig-section {
            display: flex;
            justify-content: space-between;
            margin-top: 25px;
            flex-wrap: wrap;
            gap: 20px;
        }

        .sig-block {
            width: 42%;
            min-width: 180px;
        }

        .sig-label {
            font-size: 11px;
            color: #4a148c;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .sig-line {
            border-top: 3px solid #1a237e;
            padding-top: 8px;
            margin-top: 40px;
            position: relative;
        }

        .sig-line::before {
            content: '';
            position: absolute;
            top: -3px; left: 0;
            width: 50px; height: 3px;
            background: #e91e63;
        }

        .sig-name {
            font-weight: bold;
            color: #1a237e;
            font-size: 13px;
        }

        .sig-detail {
            font-size: 10.5px;
            color: #666;
            line-height: 1.6;
        }

        /* === STAMP === */
        .stamp-area {
            margin-top: 25px;
            padding: 12px;
            border: 2px dashed #c5cae9;
            border-radius: 10px;
            text-align: center;
            min-height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 42%;
            min-width: 180px;
        }

        .stamp-text {
            font-size: 11px;
            color: #9e9e9e;
            font-style: italic;
        }

        /* === THANK YOU === */
        .thank-you {
            text-align: center;
            margin-top: 20px;
            padding: 12px;
            background: linear-gradient(135deg, #e8eaf6, #ede7f6, #fce4ec);
            border-radius: 10px;
            border: 1px solid #c5cae9;
        }

        .thank-you p {
            font-size: 14px;
            color: #1a237e;
            font-weight: bold;
            font-style: italic;
            letter-spacing: 1px;
        }

        .thank-you .small {
            font-size: 11px;
            color: #666;
            font-weight: normal;
            margin-top: 3px;
        }

        /* === FOOTER === */
        .page-footer {
            position: absolute;
            bottom: 0;
            left: 14px; right: 14px;
            z-index: 2;
        }

        .footer-bar {
            background: linear-gradient(135deg, #1a237e 0%, #4a148c 50%, #1a237e 100%);
            padding: 10px 20px;
            border-top: 3px solid #e91e63;
            text-align: center;
        }

        .footer-text {
            font-size: 9.5px;
            color: rgba(255,255,255,0.9);
            line-height: 1.6;
        }

        .footer-text strong { color: #e8b4f8; }
        .footer-text .sep { color: #e91e63; margin: 0 4px; }
    </style>
</head>
<body>

    <!-- BUTTONS -->
    <div class="no-print">
        <button class="btn" onclick="window.print()">&#128424; Print Invoice</button>
        <button class="btn btn-pdf" onclick="window.print()">&#128190; Save as PDF</button>
    </div>

    <!-- A4 PAGE -->
    <div class="page">

        <div class="bg-pattern"></div>
        <div class="side-left"></div>
        <div class="side-right"></div>
        <div class="corner-tl"></div>
        <div class="corner-tr"></div>
        <div class="corner-bl"></div>
        <div class="corner-br"></div>
        <div class="watermark-symbol">&#9764;</div>
        <div class="watermark-text">MEDICAL INVOICE</div>

        <div class="inner">

            <!-- Header -->
            <div class="header-bar">
                <div class="header-cross">
                    <span>+</span>
                </div>
                <div class="practice-name">Dr Ikponmwosa Irene Practice</div>
                <div class="medical-title">Medical Practitioner</div>
            </div>

            <!-- Badges -->
            <div class="badges">
                <span class="badge">HPCSA: MP0718181</span>
                <span class="badge purple">Practice No: 1232126</span>
                <span class="badge red">Tax Invoice</span>
            </div>

            <!-- Contact Details -->
            <div class="details-box">
                <div class="details-row">
                    <span class="label">Business Address:</span> 141 Acacia Street, Rosedale, Uitenhage, Eastern Cape
                </div>
                <div class="details-row">
                    <span class="label">Postal Address:</span> 27 Devon Place, 04 Penzance Observatory, Cape Town
                </div>
                <div class="details-row">
                    <span class="label">Phone:</span> 063 003 0044
                    <span class="sep">&#9830;</span>
                    <span class="label">Email:</span> ikbrown2001@yahoo.com
                </div>
            </div>

            <!-- Invoice Title -->
            <div class="invoice-title-banner">
                <h2>Tax Invoice</h2>
                <div class="subtitle">Medical Services Rendered</div>
            </div>

            <!-- Invoice Info -->
            <div class="invoice-info">
                <div class="info-block">
                    <div class="info-block-title">Bill To (Patient Details)</div>
                    <div class="info-field">
                        <span class="info-label">Patient Name:</span>
                        <div class="info-value"></div>
                    </div>
                    <div class="info-field">
                        <span class="info-label">ID Number:</span>
                        <div class="info-value"></div>
                    </div>
                    <div class="info-field">
                        <span class="info-label">Address:</span>
                        <div class="info-value"></div>
                    </div>
                    <div class="info-field">
                        <span class="info-label">Phone:</span>
                        <div class="info-value"></div>
                    </div>
                    <div class="info-field">
                        <span class="info-label">Medical Aid:</span>
                        <div class="info-value"></div>
                    </div>
                    <div class="info-field">
                        <span class="info-label">Member No:</span>
                        <div class="info-value"></div>
                    </div>
                </div>

                <div class="invoice-number-box">
                    <div class="inv-label">Invoice Number</div>
                    <div class="inv-number">INV-________</div>
                    <div class="inv-date">Date: ___/___/______</div>
                    <div class="inv-date" style="margin-top:8px;">Due Date: ___/___/______</div>
                </div>
            </div>

            <!-- Divider -->
            <div class="fancy-divider">
                <div class="line"></div>
                <div class="dot"></div>
                <div class="diamond"></div>
                <div class="dot"></div>
                <div class="line right"></div>
            </div>

            <!-- Invoice Table -->
            <table class="invoice-table">
                <thead>
                    <tr>
                        <th style="width:8%;">#</th>
                        <th style="width:15%;">ICD-10 / Tariff Code</th>
                        <th style="width:37%;">Description of Service</th>
                        <th style="width:10%;">Qty</th>
                        <th style="width:15%;">Unit Price (R)</th>
                        <th style="width:15%;">Amount (R)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>1</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                    </tr>
                    <tr>
                        <td>2</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                    </tr>
                    <tr>
                        <td>3</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                    </tr>
                    <tr>
                        <td>4</td>
                        <td></td>
                        <td
